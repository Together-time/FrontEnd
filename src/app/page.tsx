import React from "react";
import ProjectList from "@/components/projectList/projectList";

const Home: React.FC = () => {
    return(
        <div>
            <ProjectList />
        </div>
    );
};

export default Home;